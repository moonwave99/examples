# Keyboard Focus Manager

Navigate the columns with arrows, select multiple entries with `cmd` / `shift` / click on the entry.

Hit `Enter` to display current selection, double click on any entry to open a modal.

## Usage

```txt
npm i
npm run dev
```
